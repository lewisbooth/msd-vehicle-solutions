{
  "name": "msd-vehicle-solutions",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-static.mjs && vite --config vite.public.config.mjs",
    "build": "npm run typecheck && npm run build:public && npm run build:admin",
    "typecheck": "tsc --noEmit -p tsconfig.worker.json",
    "build:public": "node scripts/prepare-static.mjs && vite build --config vite.public.config.mjs && node scripts/copy-static.mjs && node scripts/build-public.mjs",
    "build:admin": "vite build --config vite.admin.config.mjs",
    "migrate:preview": "node scripts/migrate-preview.mjs",
    "migrate:production": "wrangler d1 migrations apply DB --remote",
    "deploy:preview": "npm run migrate:preview && wrangler preview",
    "deploy:production": "npm run migrate:production && wrangler deploy"
  },
  "dependencies": {
    "jose": "^6.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "@types/node": "^26.6.2",
    "@vitejs/plugin-react": "^5.0.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "wrangler": "^4.135.0"
  },
  "engines": {
    "node": ">=22.12"
  }
}
